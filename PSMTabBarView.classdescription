{
	Actions = {
		// Define action descriptions here, for example
		// "myAction:" = id;
	};
	Outlets = {
		delegate = id;
		tabView = NSTabView;
		partnerView = id;
	};
    ClassName = PSMTabBarControl; 
    SuperClass = NSControl; 
}
